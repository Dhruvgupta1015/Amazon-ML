{
  "name": "amazon-ml-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix business_entity_resolution/frontend",
    "build": "cd business_entity_resolution/frontend && npm install && npm run build",
    "start": "npm run start --prefix business_entity_resolution/frontend"
  }
}
